# Server port
PORT=3000

# Frontend URL for CORS
FRONTEND_URL=http://localhost:8080

# Dashboard authentication (optional - auto-generated if not set)
# DASHBOARD_USER=admin
# DASHBOARD_PASS=your-secure-password

# PostgreSQL database connection (optional - runs in-memory if not set)
# DATABASE_URL=postgres://listen:listen@localhost:5432/listen

# Song cache directory (optional - defaults to /data/songs)
# SONGS_PATH=/data/songs

# Cover art cache directory (optional - defaults to /data/covers)
# COVERS_PATH=/data/covers

# Lobby expiry time in hours (optional - defaults to 24)
# LOBBY_EXPIRY_HOURS=24

# Spotify API credentials (optional - enables Spotify link support)
# Get from https://developer.spotify.com/dashboard
# SPOTIFY_CLIENT_ID=your-client-id
# SPOTIFY_CLIENT_SECRET=your-client-secret
